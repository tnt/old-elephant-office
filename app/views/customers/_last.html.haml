%ul
  - for customer in @last_customers[0,2]
    %li{:id => "lcustomer_#{customer.id}"}= rs_link_to customer.name, customer.id.to_s, 'data-customer-id' => customer.id, 'data-loading-element' => 'customers_preview', :title => customer.name
%ul{:style => 'display: none;'}
  - if @last_customers[2,8]
    - for customer in @last_customers[2,8]
      %li{:id => "lcustomer_#{customer.id}"}= rs_link_to customer.name, customer.id.to_s, 'data-customer-id' => customer.id, 'data-loading-element' => 'customers_preview', :title => customer.name
